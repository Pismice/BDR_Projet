<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Projet_BDR</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="equipe">
                <span class="oi" aria-hidden="true"></span> Equipes
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="transferts">
                <span class="oi" aria-hidden="true"></span> Transferts
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="joueur">
                <span class="oi" aria-hidden="true"></span> Joueurs
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="arme">
                <span class="oi" aria-hidden="true"></span> Armes
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="agent">
                <span class="oi" aria-hidden="true"></span> Agents
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="carte">
                <span class="oi" aria-hidden="true"></span> Cartes
            </NavLink>
        </div>

    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
